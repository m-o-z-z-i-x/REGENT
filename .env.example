# router address
OPENWRT_HOST=192.168.1.1

# ssh user and port
OPENWRT_USER=root
OPENWRT_PORT=22

# private key, found on its own unless pointed somewhere else with an absolute path
#OPENWRT_KEY=/absolute/path/to/key

# unlocks every tool that changes the router
#OPENWRT_ENABLE_WRITE=1